# ===================================================================
# PersonaPlex Environment Configuration
# Copy this file to .env and fill in the values.
# ===================================================================

# --- HuggingFace ---
# Required: Accept the model license at https://huggingface.co/nvidia/personaplex-7b-v1
# then create a token at https://huggingface.co/settings/tokens
HF_TOKEN=hf_your_token_here

# --- Twilio (required for phone call integration) ---
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890

# --- Server ---
# Public URL of the server (used for Twilio webhook callbacks).
# For Cloud Run, this will be the service URL, e.g. https://personaplex-xxxxx-uc.a.run.app
SERVER_URL=https://your-server-url

# --- Model Defaults ---
# Default voice prompt file (from the voices directory)
DEFAULT_VOICE_PROMPT=NATF2.pt
# Default system text prompt
DEFAULT_TEXT_PROMPT=You are a wise and friendly teacher. Answer questions or provide advice in a clear and engaging way.

# --- Runtime ---
# Device: cuda or cpu
DEVICE=cuda
# HuggingFace model repo
HF_REPO=nvidia/personaplex-7b-v1
# Set to 1 to offload model layers to CPU (for GPUs with <24GB VRAM)
CPU_OFFLOAD=0
# Disable torch.compile (set to 1 if you see compilation errors)
NO_TORCH_COMPILE=1
# Enable hf_transfer for fast model downloads (Rust-based, 5-10x speedup)
HF_HUB_ENABLE_HF_TRANSFER=1
# Directory to save conversation recordings
RECORDINGS_DIR=/app/recordings
